server:
  port: 7002

spring:
  application:
    name: lotto-draw-service
  datasource:
    url: jdbc:mysql://localhost:3317/lotto_draw
    username: lotto_user
    password: lotto1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

services:
  result-service:
    url: http://localhost:7003